# Frontend - Chat App (React + Vite + Tailwind)

Este é o frontend de um sistema simples de chat em tempo real utilizando **React**, **Vite**, **TailwindCSS** e **Socket.IO Client**.

A aplicação se conecta ao backend Node.js/Express para autenticação, troca de mensagens e atualização do status online dos usuários.

---

## 🚀 Tecnologias utilizadas

- **React 18**
- **Vite**
- **TypeScript**
- **TailwindCSS**
- **Socket.IO Client**
- **Axios**
- **React Router**

---

# 📦 Instalação

Clone o projeto e instale as dependências:

```bash
git clone <YOUR_GIT_URL>
cd <YOUR_PROJECT_NAME>
npm install
```

Depois só rodar o comando:
```bash
npm run dev
```

