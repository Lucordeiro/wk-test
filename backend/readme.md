# Backend - Chat App (Node.js + Express + TypeScript)

Este é o backend de um pequeno sistema de chat em tempo real utilizando **Node.js**, **Express**, **TypeScript**, **MongoDB** e **Socket.IO**.

O projeto pode ser executado em modo desenvolvimento ou totalmente via Docker (incluindo MongoDB e Mongo Express).

---

## 🚀 Tecnologias

- Node.js
- Express
- TypeScript
- MongoDB + Mongoose
- JWT Authentication
- Socket.IO
- Docker / Docker Compose

---

# 📦 Instalação

Clone o projeto:

```bash
git clone <YOUR_GIT_URL>
cd <YOUR_PROJECT_NAME>
```
Depois precisa subir os containers do banco e API (necessário instalação docker)

```bash
docker-compose up
```
